node_modules/
# 本地测试 vault（Obsidian 插件目录，含用户 data.json，不应入库）
weixin-sync-test-vault/
# 构建产物按需入库（manifest.json / main.js / versions.json 需随发版提交），此处仅忽略 sourcemap
*.map
.DS_Store
.hotreload
# 运行期用户数据（可能含 API Key 等敏感配置），不随发版发布
data.json
# 锁文件不入库，避免发版 diff 噪音
package-lock.json
